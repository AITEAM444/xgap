# xgap 프로젝트 개요 (처음 오는 사람용)

이 문서는 `README.md`(2,400줄이 넘는 **실험 일지**)를 처음부터 다 읽지 않아도
프로젝트의 **최종 목표**와 **현재 진행 상황**을 파악할 수 있도록 정리한 요약본이다.
세부 근거·실패 기록·의사결정 과정은 전부 README에 있으니, 이 문서에서 궁금한
항목이 생기면 README의 해당 섹션 제목으로 찾아 들어가면 된다.

> 작성 기준일: 2026-08-13 / 기준 커밋: `681c86e` (branch `master`)

---

## 0. 30초 요약

- **하고 싶은 것**: 로봇 조작 정책(VLA 모델)이 한 장면에서 여러 후보 행동을
  뽑아낼 때, **후보를 잘 고르면 성능이 얼마나 올라가는가**를 측정하는 것.
  (`Oracle` = 정답을 아는 선택기, `World-model` = 예측 모델로 고르기,
  `Random` = 아무거나 — 세 조건을 같은 조건에서 비교)
- **지금 위치**: 아직 그 본 실험(= Oracle 커브 그리기)에 들어가지 못했다.
  본 실험이 의미가 있으려면 먼저 통과해야 하는 **전제 조건 검증(Gate)** 단계에 있고,
  현재 **Gate 1은 사실상 통과, Test 2는 통과, Gate 2는 진행 중(파일럿)** 상태다.
- **이 리포의 성격**: "고치는 repo"가 아니라 **"진단하는 repo"**.
  실패한 가설과 버려진 접근까지 전부 기록에 남기는 것이 이 프로젝트의 규칙이다.
- **마감**: 게이트 심사가 **2026년 9월 초** — 기준일 기준 약 3주 남았다.

---

## 1. 최종 목표 (무엇을 증명하려는 프로젝트인가)

로봇 정책(policy)에게 "지금 상황에서 어떻게 움직일래?"라고 물으면, 같은 장면에서도
매번 조금씩 다른 행동 후보(candidate)를 내놓는다. 이 프로젝트의 질문은 이것이다:

> **여러 후보 중에서 "좋은 것"을 골라줄 수 있다면, 로봇의 성공률은 얼마나 올라가는가?
> 그리고 그 선택을 세계 모델(world model)이 대신해 줄 수 있는가?**

이걸 측정하려면 결국 아래 그림(**Oracle 커브**)을 그려야 한다.

```
성공률
  ▲
  │            ● Oracle       (후보 N개 중 실제로 성공하는 걸 골라준 경우 = 상한선)
  │        ●
  │    ●   ○   ○ World-model  (예측 모델이 고른 경우 = 우리가 만들려는 것)
  │  ● ○ ×   ×  × Random      (그냥 아무거나 = 하한선)
  │ ○×
  └──────────────────────────► 후보 개수 N (1, 2, 4, 8, ...)
```

- **Oracle 커브가 위로 올라가지 않으면** → 후보를 아무리 잘 골라도 소용없다는 뜻이고,
  이 연구 주제 자체가 성립하지 않는다.
- **Oracle과 Random 사이에 큰 간격이 있으면** → 그 간격이 곧 "world model이 채울 수 있는
  잠재적 이득"이고, 그게 이 연구의 존재 이유가 된다.

그래서 **본 실험(Oracle/World-model/Random 비교)에 들어가기 전에**, 아래 전제들이
실제로 성립하는지부터 하나씩 검증하는 것이 지금까지의 모든 작업이다.

| 게이트 | 질문 | 실패하면? |
|---|---|---|
| **Gate 1** | 쓰려는 정책이 이 태스크에서 "적당히" 성공하는가? (너무 못해도, 너무 잘해도 안 됨) | 측정할 여지 자체가 없음 |
| **Test 2** | 같은 분기점에서 여러 후보를 **공정하게** 비교할 수 있는가? (재현성) | 후보 비교가 노이즈 측정이 됨 |
| **Gate 2** | 정책이 실제로 **의미 있게 다른** 후보들을 내놓는가? | 고를 게 없으니 커브를 그릴 이유가 없음 |

---

## 2. 용어 사전 (배경지식 없이 읽기 위한 최소한)

| 용어 | 뜻 |
|---|---|
| **LIBERO** | 로봇 조작 벤치마크(시뮬레이터). "그릇을 접시 위에 올려라" 같은 태스크 모음. MuJoCo 물리엔진 기반 |
| **libero_spatial / libero_10** | LIBERO의 태스크 묶음(suite). `spatial`은 물체 1개 단발 pick&place(쉬움), `libero_10`(=LIBERO-Long)은 물체 2개 순차 작업(어려움) |
| **SmolVLA** | 이 프로젝트가 검증 대상으로 쓰는 소형 VLA(Vision-Language-Action) 정책. 체크포인트: `HuggingFaceVLA/smolvla_libero` |
| **lerobot** | HuggingFace의 로봇 학습 라이브러리. 정책 로딩·환경 래핑·평가 CLI(`lerobot-eval`)를 제공 |
| **episode / rollout** | 한 번의 시도. 성공하거나, `max_steps`(예: 520 스텝)까지 가서 실패로 끝남 |
| **action chunk** | 정책이 한 번 호출될 때 뱉는 여러 스텝 분량의 행동 묶음(SmolVLA는 50스텝) |
| **candidate(후보)** | 같은 관측에서 정책을 여러 번 샘플링해 얻은 서로 다른 action chunk들 |
| **branch point / prefix replay** | 여러 후보를 같은 지점에서 비교하기 위해, 기록된 행동을 처음부터 N스텝 되돌려 실행해 정확히 같은 상태에 도달시키는 방법 |
| **exec_horizon** | 후보를 골랐을 때 실제로 몇 스텝을 실행에 옮기는지 |
| **Gate** | 본 실험 진입 전에 통과해야 하는 검증 관문 (위 표 참고) |

---

## 3. 리포지토리 지도

```
xgap/
├── xgap_code/          핵심 라이브러리 (시뮬레이터 없이도 import 되도록 설계)
│   ├── config.py         모든 실험 파라미터의 dataclass 정의 + 검증 로직
│   ├── harness.py        LIBERO 환경 생성 (+ 테스트용 MockLiberoEnv)
│   ├── policy_rollout.py 정책을 실제로 굴리는 루프 (라이브 롤아웃)
│   ├── replay.py         기록된 행동 재생 (demo replay / prefix replay)
│   ├── logging_schema.py 에피소드 단위 parquet 저장 + 재시작(resume) + Drive 동기화
│   ├── dataset_io.py     HuggingFaceVLA/libero 데이터셋에서 필요한 컬럼만 읽기
│   ├── diversity_metrics.py / gripper_metrics.py / state_determinism.py  순수 계산 로직
│   └── plots.py, metering.py
├── configs/            실험 1개 = yaml 1개 (코드에 상수 하드코딩 금지가 규칙)
├── scripts/            실행 단위는 노트북 셀이 아니라 CLI 스크립트
├── tests/              시뮬레이터 없이 도는 pytest 75개 (MockLiberoEnv 기반)
├── notebooks/          Colab에서 scripts/만 호출하는 얇은 래퍼
├── outputs/            실험 결과 (원칙적으로 gitignore, 단 일부는 업로드되어 있음 — §5 참고)
├── setup_colab.sh      Colab 세션마다 돌리는 멱등 환경 구축 스크립트
└── README.md           ★ 실제 실험 일지. 이 문서의 모든 주장의 근거가 여기 있다
```

**실행 환경**: 개발은 Windows 로컬, 실제 실행은 **Google Colab(GPU)** + Google Drive.
Colab 세션이 끊겨도 결과가 날아가지 않도록, 에피소드 하나가 끝날 때마다 parquet로
저장하고 N개마다 Drive로 일괄 복사한다(`EpisodeStore`). `resume: true`면 이미 끝난
에피소드는 건너뛰므로, 같은 명령을 다시 실행하면 이어서 돈다.

---

## 4. 지금까지의 여정 (결론 중심 요약)

README는 시간순 일지라서 길다. 큰 줄기만 5막으로 요약하면 다음과 같다.

### 1막 — "정책이 물건을 못 잡는다"에서 출발
원래 보고된 증상: *"팔이 물체 근처까지는 그럴듯하게 가는데, 잡지를 못한다."*
원인 후보를 H1~H4로 세워두고 시작했다 (실행 granularity, 손목 카메라 입력 누락,
그리퍼 부호 규약, 상대/절대 제어 모드).

### 2막 — 인프라부터 의심하고, 인프라를 무죄로 확정
- 기록된 데모 행동을 그대로 재생(demo replay)했더니 **0/10 실패**.
- 하지만 다른 정책(`pi05_libero_finetuned`)을 공식 CLI로 돌리니 **20/20 성공**.
  → 설치·MuJoCo·렌더링·성공 판정 로직은 **정상**.
- 원인은 이 프로젝트가 데이터셋의 데모 순번을 LIBERO의 초기상태(init_state) 인덱스로
  **잘못 매핑**한 것이었음을 50-way 스윕으로 확정(데모 0은 50개 중 3번, 23번에서만 성공).
- **그 매핑을 끝까지 찾는 일은 의도적으로 포기**했다. 정책 평가는 애초에 데모의
  초기상태를 쓰지 않고 `init_states`를 0번부터 순서대로 쓰기 때문에, 이 매핑은
  진단 도구(demo replay)에만 필요한 문제였기 때문. (README: "Mapping search abandoned")

### 3막 — 문제는 태스크 난이도였다
xgap 코드를 완전히 배제하고 순수 `lerobot-eval`로 SmolVLA를 직접 평가:

| 조건 | 결과 |
|---|---|
| `libero_10`, n_action_steps=1 | **0/5** |
| `libero_10`, n_action_steps=10 | **0/5** (→ 실행 granularity 가설 H1 기각) |
| `libero_10`, 렌더 해상도 256 강제 | 1/5 (판단 불가, 노이즈 범위) |
| **`libero_spatial`, n_action_steps=10** | **3/5 (60%)** ← 결정적 |

→ 체크포인트도 환경도 고장난 게 아니라, **이 체크포인트가 `libero_10`(장기 2단계 태스크)에
특히 약한 것**. 영상에서 그리퍼가 "닫았다 열었다"를 반복하는(주저하는) 모습이 관찰됨.
"정책 입력 이미지가 좌우 반전된 것 아니냐"는 반박 가설도 직접 PNG를 떠서 비교해 **기각**.

### 4막 — 실험 대상 suite를 LIBERO-Spatial로 변경
원 제안서는 "쉬운 suite는 이미 98%로 포화됐다"며 LIBERO-Long을 골랐지만,
**실제로 측정한 숫자**를 제안서 자신의 기준에 대입하면 결론이 뒤집힌다:

| | LIBERO-Long | LIBERO-Spatial |
|---|---|---|
| 최고 성능 모델들 | 92~97% | >98% |
| **SmolVLA 실측** | **0%** | **60%** |
| 실험 기반으로 쓸 수 있나? | 불가 (아예 못 함) | **가능** |

→ 8월 게이트는 **libero_spatial**로 진행. LIBERO-Long은 "이 체크포인트는 장기 2단계
태스크를 안정적으로 수행하지 못해 제외했다"고 정직하게 기술하고 확장 실험으로 남김.

### 5막 — 게이트 검증 (현재 위치)
- **Gate 1**: xgap 자체 harness로 정책을 실제로 굴려 성공률 측정 (§5)
- **Test 2**: 후보 비교의 재현성 확보 — **state save/restore는 실패, prefix replay로 설계 변경 후 성공**
- **Gate 2**: 후보들이 실제로 의미 있게 다른가 — **진행 중**

---

## 5. 현재 상태 상세

### Gate 1 — 정책 baseline 성능 (libero_spatial, 태스크당 20 에피소드)

목적: 성능이 **너무 낮지도(측정 불가) 너무 높지도(천장 효과)** 않은지 확인.
기준: **≥90%면 Gate 1 실패**(개선 여지가 없어서 실험 substrate로 못 씀).

| task | 성공 | 비율 | 출처 |
|---|---|---|---|
| 0 | 12/20 | 60% | README 기록 |
| 1 | (원본 데이터 리포에 없음) | — | README에 "20개 중 19개가 rollout_length 95~121로 거의 동일"이라는 언급만 있음 |
| 2 | (원본 데이터 리포에 없음) | — | Gate 2 diversity 실행에 사용됨 |
| 3 | 15/20 | 75% | `outputs/task3/` |
| 4 | 16/20 | 80% | `outputs/task4/` |
| 5 | **9/20** | **45%** | `outputs/task5/` |
| 6 | 18/20 | **90%** | `outputs/task6/` |
| 7 | 16/20 | 80% | `outputs/task 7/` |
| 8 | 18/20 | **90%** | `outputs/task 8/` |
| 9 | 17/20 | 85% | `outputs/task9/` (+ `rollout_summary.json`) |

> ⚠️ **task 3~9 수치는 이 문서를 쓰면서 `outputs/`의 parquet 파일을 직접 집계한 값이고,
> README에는 아직 기록되어 있지 않다.** README의 서술은 task 0~2까지만 반영된 상태다.

이 표에서 새로 온 사람이 바로 알아야 할 두 가지:

1. **task 6과 task 8이 정확히 90%** — Gate 1의 실패 기준선에 걸쳐 있다. 실험에 쓸
   태스크를 고를 때 이 둘을 포함할지는 **아직 결정되지 않은 열린 질문**이다
   (n=20에서 90%는 에피소드 2개 차이라 통계적으로도 애매하다).
2. **실패 에피소드는 전부 `rollout_length=520`** — 즉 성공하지 못하고 스텝 상한까지
   끝까지 돌았다는 뜻. 성공은 보통 80~150 스텝에서 끝난다. 그래서 **실패가 비쌀수록
   실행 시간이 폭증한다**(task 9 실측: 성공 평균 226초 vs 실패 평균 734초). 실험 비용
   산정은 항상 이 비대칭을 전제로 해야 한다.

**분산 실행 구조**: `configs/policy_rollout_libero_spatial_task{3..9}.yaml`은 팀원 한 명당
태스크 하나씩 나눠 돌리기 위한 복사본이다. `remote_output_root`가 전부 동일해서,
같은 Drive 폴더를 공유하면 결과가 자동으로 합쳐진다(파일명에 task_id가 들어가 충돌 없음).

### Test 2 — 후보 비교의 재현성: **통과 (설계 변경 후)**

원래 계획은 "시뮬레이터 상태를 저장했다가 복원해서 후보마다 되돌리기"였다. 그런데:

- 저장/복원 시 상태가 완전히 같지 않았다. 판정 기준은 절대 오차가 아니라
  **신호/노이즈 비율**(서로 다른 후보가 만드는 차이 ÷ 복원 오차)이었고, **최소 1.0배**
  — 즉 후보 비교가 노이즈 측정과 구분되지 않는 수준으로 **실패**.
- 원인 후보를 순서대로 검증: `qacc_warmstart`(솔버 warm-start) → **기각**,
  컨트롤러 내부 Python 상태 → **기각**. MuJoCo API 수준의 한계로 판단.
- **설계 변경**: 상태 복원을 포기하고 **prefix replay**로 전환. 후보마다
  `env.reset(seed)` → 기록된 prefix 행동 재생 → 거기서부터 후보 실행.
  복원 단계가 아예 없으니 복원 오차도 없다.
- 결과: **모든 스텝에서 정확히 0.0 차이(bit-identical), 3 trial 전부 통과.**

**여기서 나온 구속력 있는 규칙**: 후보마다 **환경 인스턴스를 새로 만들어야 한다**.
한 인스턴스에 `reset()`을 여러 번 호출해 재사용하면 (내부 카운터 때문에) 최대 0.021의
비결정성이 조용히 되살아난다 — 에러도 안 나고 결과만 오염된다.

**비용**: 분기점이 뒤에 있을수록 후보마다 prefix를 다시 실행해야 하므로 비싸다.
후보 1개당 대략 "prefix 길이 + 후보 길이"만큼의 시뮬레이션이 든다.

### Gate 2 — 후보 다양성: **진행 중 (설계를 세 번 고쳤음)**

질문: 정책이 같은 관측에서 정말 **다른** 후보들을 내놓는가? 그리고 그 차이가
**결과(성공/실패)를 바꾸는가?**

지금까지 밝혀진 것:

- **행동 값 수준의 다양성은 실재한다**: 후보 64개 간 평균 pairwise L2 ≈ 4.2~4.6,
  그리퍼 채널은 열기/닫기가 거의 50:50으로 갈림. 분기점을 뒤로 옮기면
  "닫으라고 명령하는 후보 비율"이 0.53 → 0.85로 변함 = **언제 잡을지가 후보마다 다르다**.
- **그런데 "팔이 도착한 위치의 분산"으로 재는 지표(endpoint variance)는 폐기됐다.**
  이유가 명확하다: `exec_horizon=10`(10스텝 실행)인데 그리퍼가 실제로 닫히는 데는
  15~20스텝이 걸린다. 차이가 물리적으로 드러날 시간 자체가 없었던 것 — 분기점을
  옮겨서 해결될 문제가 아니라 **지표와 메커니즘의 구조적 불일치**.
- **그래서 Gate 2의 판정은 오직 "에피소드 성공/실패"로 한다.**
  (`scripts/run_gate2_outcome.py`)

**판정 규칙**:
- `mixed` = 한 분기점에서 뽑은 후보들의 결과에 성공과 실패가 **둘 다** 있음
- `is_primary_signal` = `mixed` **이면서** 원본 에피소드가 **실패** 에피소드였음
  → *"원래는 실패할 궤적이었는데, 결정적 순간에 좋은 후보를 고르니 성공으로 뒤집혔다"* —
  이게 Oracle 커브가 baseline 위로 올라가는 **바로 그 메커니즘**이다.
- **Gate 2 통과 조건**: 테스트한 지점 중 **단 하나라도** `is_primary_signal`이 참이면 통과.

**여기서 잡힌 설계 버그 3개** (전부 실제 실행 전/직후에 발견, README에 상세 기록):
1. 후보 5개로는 "다양성 없음"을 결론지을 수 없다 → **64개**로 상향 (섞인 결과가
   나오는 순간 조기 종료하는 건 유지)
2. 분기 원본이 항상 **성공** 에피소드였다 → 좋은 궤적 중간에서 갈라지면 뭘 골라도
   성공하니 아무것도 측정하지 못함. → **실패 에피소드에서 분기해야 한다**
3. 분기점을 `길이의 50%`로 잡았다 → 실패 에피소드는 길이가 520이라 **260 스텝**에서
   분기됨. 성공 궤적은 66~121 스텝에 끝나므로, 이미 한참 전에 실패가 확정된 지점이었다.
   실제로 **26/26 후보가 전부 실패**. → **고정 절대 스텝 `prefix_length=40`**으로 변경
   (모든 궤적이 통과하는 잡기 직전 구간). 이제 `prefix_length + exec_horizon < max_steps`를
   config 로드 시점에 검증해서 같은 부류의 버그가 조용히 넘어가지 못하게 막았다.

**현재 실행해야 할 것**: `configs/gate2_outcome.yaml` — **파일럿** (task 0,
실패 seed 2 + 성공 seed 0, 후보 64개). 실패 seed 하나만으로도 통과/실패 판정은 난다.
통과하면 `configs/gate2_outcome_full.yaml`(4 task × 4 seed)로 확장.

> 비용 주의: 잘못된 분기점(260스텝)에서 후보 1개당 265~300초가 들었다. `prefix_length=40`으로
> 바꾼 뒤의 실제 비용은 아직 미측정 — 실행 로그의 `candidate=k/n ... elapsed=` 줄을 보고
> 실측해야 한다. full config 기준 예전 추정치는 **~74시간**이었다.

### 리포 상태에 관한 실무 메모

- `.gitignore`는 `outputs/`를 제외하도록 되어 있는데, **task 3~9 결과 141개 parquet은
  실제로 커밋되어 있다**(팀원들이 결과를 업로드하는 경로로 쓰인 듯). 의도된 예외인지
  확인이 필요하다.
- 디렉터리 이름이 통일되어 있지 않다: `task3`/`task6` vs `task 7`/`task 8`(공백 포함),
  그리고 `task4`/`task5`/`task9`만 `episodes/` 하위 폴더 구조를 가진다.
  집계 스크립트를 쓸 때 재귀 탐색이 필요하다.
- `outputs/task9/`에만 `rollout_summary.json`이 있고 나머지는 parquet만 있다.

---

## 6. 다음에 해야 할 일 (우선순위 순)

1. **Gate 2 파일럿 실행** — `scripts/run_gate2_outcome.py` + `configs/gate2_outcome.yaml`.
   지금 프로젝트 전체를 막고 있는 단 하나의 항목이다. 통과하면 Oracle 커브로 진입.
2. **Gate-1 결과를 README에 반영** — task 3~9 실측치(§5)가 아직 일지에 없다.
   실험에 쓸 태스크 최종 선정(특히 90%인 task 6, 8을 포함할지)도 함께 결정해야 한다.
3. **Gate 2 통과 시**: Oracle / World-model / Random 세 조건 구현 → N 축 스윕 →
   Oracle 커브 작성. `n_decision_points`, `exec_horizon`, `selection_unit`은 이미
   config 필드로 존재한다.
4. **(확장, 9월 게이트 범위 밖)** (N, H) 2차원 스윕, LIBERO-Long 복귀 —
   단 Long은 체크포인트 추가 파인튜닝이 선행되어야 함(현재 0% baseline).

---

## 7. 절대 어기면 안 되는 설계 규칙

README 마지막 섹션("Design constraints encoded in this codebase")에 명시된 것들.
전부 **실제로 사고가 난 뒤에** 규칙이 된 항목들이다.

1. **후보마다 환경 인스턴스를 새로 만든다.** 한 인스턴스를 `reset()`으로 재사용하면
   비결정성이 조용히 되살아난다(Test 2에서 실측 확인).
2. **후보 비교는 prefix replay로 한다.** state save/restore는 이 프로젝트에서 실패로
   판정된 접근이다.
3. **`n_decision_points`는 Oracle/World-model/Random 세 조건에 동일하게 적용한다** —
   관례가 아니라 코드로 강제.
4. **`exec_horizon`과 `selection_unit`은 별개 필드로 유지한다.** 스텝 단위 선택은
   chunk 내부 구조를 깨서 가짜 성능 하락을 만든다.
5. **`lerobot`/`libero`를 monkey-patch하지 않는다.** 불가피하면 별도 모듈로 격리하고
   이유를 주석으로 남긴다.
6. **실험 파라미터는 전부 `configs/*.yaml`에.** `xgap_code/`와 `scripts/`에 상수
   하드코딩 금지.

---

## 8. 처음 온 사람이 30분 안에 할 수 있는 것

```bash
# 1) 시뮬레이터 없이 도는 테스트 (로컬 Windows/Mac에서 그냥 됨)
pip install -r requirements.txt
pytest          # MockLiberoEnv 기반 75개 테스트

# 2) 실제 실행은 Colab에서
#    notebooks/run_replay.ipynb 를 열고 위에서부터 실행
#    (Drive 마운트 → repo clone/pull → setup_colab.sh)
```

읽는 순서 추천:

1. 이 문서
2. `xgap_code/config.py` — 실험에 어떤 파라미터가 있는지가 곧 프로젝트의 설계도다
3. `scripts/run_gate2_outcome.py`의 모듈 docstring — 지금 진행 중인 작업의 전체 맥락과
   버그 3개의 서사가 압축되어 있다
4. `README.md`의 "Design constraints" 섹션 (맨 마지막) → 그다음 관심 있는 섹션 역순으로

---

## 9. 이 프로젝트의 작업 방식 (문화)

새로 합류하면 가장 먼저 체감하게 될 부분이라 따로 적어둔다.

- **README는 문서가 아니라 실험 일지다.** 성공만 적지 않는다. 기각된 가설, 잘못
  세웠던 설계, 실행 전에 잡은 버그까지 *왜 그렇게 판단했는지*와 함께 남긴다.
- **추측한 것과 확인한 것을 문장 단위로 구분한다.** "소스에서 확인함(verified from
  source)" / "확인 안 됨(unverified, flagged)"이 명시적으로 표기된다.
- **실제 측정 없이 크기를 정하지 않는다.** 실행 시간, 에피소드 수, 후보 수 모두
  실측 timing에서 역산한다.
- **비싼 실행 전에 설계 버그를 먼저 잡는다.** Gate 2에서 74시간짜리 실행을 하기 전에
  버그 3개를 잡은 것이 대표적인 사례다.
- **막다른 길은 실패가 아니라 설계 변경으로 기록한다.** state save/restore → prefix
  replay 전환이 그 예. 목표는 "상태 복원"이 아니라 "공정한 후보 비교"였고, 수단을
  바꿔서 목표를 달성했다.
